# Allow npm to resolve peer dependencies the legacy way.
# Required because react-scripts@5 predates TypeScript 5 and i18next 25
# and their peer dep ranges don't overlap with the installed versions.
legacy-peer-deps=true
