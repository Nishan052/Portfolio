# Prevents react-scripts from treating ESLint warnings as build errors in CI.
# Cloudflare Pages sets CI=true automatically, which causes warnings to fail builds.
# This env var is read by react-scripts before CI is checked.
DISABLE_ESLINT_PLUGIN=true
